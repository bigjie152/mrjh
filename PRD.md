# 每日计划与复盘 PRD

## 1. 项目定位

「每日计划与复盘」是一个个人自用的全栈 Web App，用于长期记录每天的计划安排、实际完成情况和复盘结论。

它参考《认知觉醒》每日计划表的核心方法：先写计划，再写实际，最后对照偏差。正式项目会参考现有前端原型的视觉风格，但代码会重新搭建，目标是成为一个可部署、可长期保存数据、可持续迭代的完整应用。

## 2. 总目标

构建一个部署在 Cloudflare 上的个人每日计划与复盘全栈系统。它应当拥有中文友好的界面、流畅的记录体验、稳定的历史数据保存能力，并能帮助用户持续观察「计划完成」与「实际完成」之间的差异。

## 3. 目标用户

主要用户是项目所有者本人。

典型使用方式：

- 早上打开网页，写下今日核心待办和计划时间块。
- 白天或晚上补充真实完成情况。
- 睡前查看计划和实际差异，写下复盘。
- 周末或月末查看历史记录和统计趋势。

## 4. 核心问题

用户需要解决的问题：

- 每天计划与实际经常不一致，但缺少长期记录。
- 任务耗时预估不准，难以复盘原因。
- 纸质表格不方便搜索、统计和长期保存。
- 浏览器本地存储不够可靠，换设备或清数据会丢失。

## 5. 产品原则

### 5.1 全中文友好

- 页面、按钮、表单、空状态、错误提示都使用中文。
- 文案要具体、自然、可执行。
- 可以保留少量装饰性英文，但不能影响主要信息理解。

### 5.2 交互流畅

- 高频操作要少点击。
- 添加时间块、复制计划、勾选待办要即时响应。
- 避免频繁使用浏览器原生弹窗。
- 输入过程不应因保存而卡顿。

### 5.3 数据可靠

- 正式数据保存到 Cloudflare D1。
- 前端可以使用本地缓存提升体验，但不能把 localStorage 当作唯一数据源。
- 需要支持导出备份。
- 后续 schema 变更要考虑迁移。

### 5.4 视觉延续

- 参考原型路径：`references/ui-prototype/每日计划与复盘`。
- 保留纸张感、手帐感、左右对照、温润配色。
- 正式实现要更克制、更稳定，适合长期每天使用。

## 6. MVP 范围

### 6.1 今日计划页

必须支持：

- 选择日期。
- 自动显示星期。
- 创建当天记录。
- 编辑 1-6 个核心待办。
- 勾选待办完成状态。
- 添加计划时间块。
- 添加实际时间块。
- 时间块支持开始时间、结束时间、内容、分类、关联待办。
- 实际时间块支持填写偏差原因。
- 单条计划复制为实际记录。
- 全部计划复制为实际记录。
- 自动计算计划耗时、实际耗时和差额。
- 写每日复盘：最大偏差、明日改进、随笔备注。

### 6.2 历史记录页

必须支持：

- 按日期倒序查看历史记录。
- 搜索任务、时间块、复盘文字。
- 打开某一天继续编辑。
- 复制历史某天作为今日计划模板。
- 删除某天记录，并有二次确认。

### 6.3 统计页

必须支持：

- 连续记录天数。
- 核心待办完成率。
- 累计实际记录时长。
- 计划总时长与实际总时长差异。
- 分类时间投入对比。
- 最近 7 天趋势。

### 6.4 数据能力

必须支持：

- 数据持久化到 Cloudflare D1。
- 本地开发可使用 D1 local 模式或等价方案。
- 支持导出 JSON 备份。
- API 返回明确中文错误信息或错误码映射。

## 7. 非 MVP 范围

第一版不做：

- 多用户协作。
- 分享广场。
- 复杂 AI 自动分析。
- 原生移动 App。
- 支付。
- 公开内容发布。

## 8. 推荐全栈架构

### 8.1 前端

- React
- TypeScript
- Vite
- Tailwind CSS
- lucide-react

前端职责：

- 呈现今日、历史、统计页面。
- 提供流畅输入体验。
- 做基础表单校验。
- 调用后端 API。
- 使用本地缓存减少等待感。

### 8.2 后端

推荐使用 Cloudflare Workers 或 Pages Functions。

后端职责：

- 提供每日记录 CRUD API。
- 执行数据校验。
- 访问 D1 数据库。
- 后续支持导入、导出、迁移。

### 8.3 数据库

推荐使用 Cloudflare D1。

原因：

- 适合结构化历史记录。
- 与 Cloudflare 部署链路匹配。
- 便于后续做统计查询。
- 对个人项目成本和维护负担较低。

### 8.4 鉴权

第一版建议使用 Cloudflare Access 保护整个站点。

这样可以先避免自建账号系统，把精力放在记录、复盘和数据保存。后续如果需要多设备或多用户，再考虑应用内登录。

## 9. 数据模型草案

### 9.1 daily_entries

```text
id
date
weekday
biggest_deviation
improvement
general_notes
created_at
updated_at
```

### 9.2 tasks

```text
id
entry_id
position
text
completed
notes
created_at
updated_at
```

### 9.3 time_blocks

```text
id
entry_id
kind              // planned 或 actual
start_time
end_time
task_position
content
category
duration_minutes
reason
created_at
updated_at
```

第一版可以先使用三张表。后续如果需要更强的统计能力，再拆分类表或模板表。

## 10. API 草案

```text
GET    /api/entries
GET    /api/entries/:date
PUT    /api/entries/:date
DELETE /api/entries/:date
GET    /api/stats/summary
GET    /api/export/json
POST   /api/import/json
```

设计原则：

- 按日期作为主要查询入口。
- 保存每日记录时可以整体 PUT，降低前端同步复杂度。
- 后端负责校验时间格式、任务数量、分类枚举。

## 11. Cloudflare 部署规划

推荐流程：

1. 使用 Git 管理项目。
2. 推送到 GitHub 仓库 `bigjie152/mrjh`。
3. Cloudflare 连接 GitHub 仓库。
4. 创建 D1 数据库。
5. 配置生产环境绑定。
6. 每次 push 自动部署。

部署目标：

- 前端静态资源由 Cloudflare 托管。
- API 在 Cloudflare Worker / Pages Functions 中运行。
- 历史记录保存在 Cloudflare D1。

## 12. 验收标准

### 12.1 产品验收

- 可以创建、编辑、保存每日记录。
- 刷新页面后数据从后端恢复。
- 可以查看历史记录。
- 可以搜索历史内容。
- 可以看到基础统计。
- 可以导出 JSON。
- 主要界面全部中文友好。
- 常用操作无明显卡顿。

### 12.2 工程验收

- 项目可通过 `npm install` 安装依赖。
- 项目可本地启动前端和后端。
- 类型检查通过。
- 生产构建通过。
- Cloudflare 部署成功。
- `.env`、密钥、数据库凭据不会进入 Git。

## 13. 里程碑

### M0：项目整理

- 初始化 Git。
- 配置 GitHub 远程。
- 清理旧原型生成物。
- 保留 UI 参考。
- 完成 PRD 和 README。

### M1：全栈脚手架

- 创建正式目录结构。
- 配置 React + Vite。
- 配置 Cloudflare Workers / Pages Functions。
- 配置 D1 本地开发。

### M2：数据层和 API

- 创建 D1 schema。
- 实现每日记录 CRUD。
- 实现基础统计 API。
- 实现 JSON 导出。

### M3：前端主流程

- 复刻参考原型的今日对照 UI。
- 接入 API 保存记录。
- 完成历史页。
- 完成统计页。

### M4：体验打磨和部署

- 优化中文文案。
- 优化移动端布局。
- 优化加载、保存、错误状态。
- 接入 Cloudflare 部署。
- 写部署文档。

## 14. 待确认问题

- 是否只给本人使用，还是未来可能多用户。
- 是否使用 Cloudflare Access 做访问保护。
- 是否需要移动端作为主要入口。
- 是否需要 PDF 导出。
- 是否需要 AI 自动总结复盘。
- 是否需要端到端加密或本地加密。

